/**
 * Sunhoney-style merchandising for the shop (`/products`) and product detail (`/products/[slug]`) only:
 * Playfair via `font-serif`, ALL CAPS, wide tracking, dusty pink — copy under sharp-cornered images.
 * Homepage carousel / favorites use script display fonts instead; do not import this there.
 */
export const sunhoneyProductNameClass =
  'font-serif font-light uppercase tracking-[0.18em] text-[color:var(--sunhoney-pink)] text-center text-xs leading-snug sm:text-sm sm:tracking-[0.22em]';

export const sunhoneyProductPriceClass =
  'font-serif font-light uppercase tracking-[0.2em] text-[color:var(--sunhoney-pink)] text-center text-xs sm:text-sm sm:tracking-[0.24em]';

export const sunhoneySectionHeadingClass =
  'font-serif font-light uppercase tracking-[0.2em] text-[color:var(--sunhoney-pink)]';

export const sunhoneyPDPTitleClass =
  'font-serif font-light uppercase tracking-[0.14em] text-[color:var(--sunhoney-pink)] text-2xl leading-snug sm:text-3xl md:text-4xl md:tracking-[0.18em]';
